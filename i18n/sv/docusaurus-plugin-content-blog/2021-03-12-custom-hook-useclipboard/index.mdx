---
slug: custom-hook-useclipboard
title: "Custom hook: useClipboard"
description: Förpacka navigator.clipboard.readText i en custom hook som returnerar klippbordets innehåll och en read-funktion.
authors: [filip]
tags: [react]
---

import { UseClipboardDemo } from "@site/blog/2021-03-12-custom-hook-useclipboard/UseClipboardDemo"

För ett tag sedan skrev jag ett [blogginlägg](/blog/what-have-i-copied) om hur man får tillgång till klippbordet med _navigator_-API:t. Jag bestämde mig för att göra det till en custom hook för att det skulle bli superlätt att använda. Innan vi går in i koden – klicka på knappen för att läsa ditt klippbord:

<UseClipboardDemo />

{/* truncate */}

Webbläsaren frågar om åtkomst till klippbordet första gången. När du har sagt ja ska du se ditt klippbordsinnehåll ovanför.

Här är hooken:

```ts
import { useState } from "react"

export const useClipboard = () => {
	const [clipboard, setClipboard] = useState<string>()

	const read = async () => {
		try {
			const value = await navigator.clipboard.readText()
			setClipboard(value.trim())
		} catch {
			setClipboard("Couldn't access clipboard...")
		}
	}

	return [clipboard, read] as const
}
```

<br />

Och så här enkelt är det att använda:

```tsx
import { useClipboard } from "hooks/useClipboard"

const Clipboard = () => {
	const [clipboard, read] = useClipboard()

	return (
		<>
			<button onClick={read}>Visa klippbord</button>
			<p>{clipboard ?? "Inget att visa..."}</p>
		</>
	)
}

export default Clipboard
```

<br />

Läsningen av klippbordet sker on demand när användaren klickar på knappen. Det undviker permission-prompts vid sidladdning och matchar hur webbläsare vill att API:t ska användas – klippbordsläsningar är tänkta att vara knutna till en användarinteraktion.
