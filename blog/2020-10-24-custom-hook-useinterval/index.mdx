---
slug: custom-hook-useinterval
title: "Custom hook: useInterval"
description: Wrap setInterval in a custom React hook that cleans up automatically and pauses when the delay is null — no extra state required.
authors: [filip]
tags: [react]
---

import { Counter } from "./Counter"

`setInterval` runs a callback at a fixed cadence. Using it in React means remembering to call `clearInterval` on unmount, and reaching for extra state any time the interval should pause or restart. This post wraps both concerns in a small custom hook called `useInterval`.

## TL;DR

- [`useEffect`](https://react.dev/reference/react/useEffect) starts the interval and returns a cleanup that calls [`clearInterval`](https://developer.mozilla.org/en-US/docs/Web/API/clearInterval) — no manual unmount handling.
- Passing `null` as the delay pauses the interval, so you can derive "running" straight from props or state instead of tracking it separately.
- Changing the delay restarts the interval automatically, so a dynamic value "just works".

{/* truncate */}

Try changing the delay below — under 50 ms the counter pauses, and raising it again continues from where it left off:

<Counter />

## The hook

```ts
import { useEffect } from "react"

export const useInterval = (callback: () => void, delay: number | null) => {
	useEffect(() => {
		if (delay === null) return
		const id = setInterval(callback, delay)
		return () => clearInterval(id)
	}, [callback, delay])
}
```

The whole thing is one effect. While `delay` is a number, an interval is running; when it becomes `null`, the early return skips the setup and the previous cleanup tears the timer down. Whenever `callback` or `delay` change, React runs the cleanup from the previous render and starts a fresh interval — that's why a dynamic delay needs no special handling.

## Using the hook

The simplest call site has the same API as `setInterval`, but without any cleanup boilerplate:

```tsx
useInterval(() => {
	console.log("Another second!")
}, 1000)
```

The hook really shines when the delay should change at runtime, or when the interval should pause based on some other state. Here's the full component behind the counter above:

```tsx
import { useInterval } from "hooks/useInterval"
import { type ChangeEvent, useState } from "react"

function Counter() {
	const [delay, setDelay] = useState(500)
	const [count, setCount] = useState(0)

	useInterval(
		() => setCount((prev) => prev + 1),
		delay >= 50 ? delay : null,
	)

	function handleDelayChange(e: ChangeEvent<HTMLInputElement>) {
		setDelay(parseInt(e.target.value, 10))
	}

	return (
		<div>
			<label>
				Choose delay [ms]
				<br />
				<input type="number" onChange={handleDelayChange} value={delay} />
			</label>
			<p>{count}</p>
		</div>
	)
}
```

Conditionally passing `null` is what makes the pause work — `useInterval` sees the delay change, tears down the old timer, and doesn't start a new one until the value is a number again. No `isRunning` flag, no extra `useEffect`, just one expression.

## Where it's used on this blog

`useInterval` powers a handful of other posts here:

- [Birthday maths](/blog/birthday-maths) — keeps the "time since" counters ticking.
- [Letter game](/blog/letter-game) — drives the in-game clock.
- [Alphabet game](/blog/alphabet-game) — drives the per-letter timer.
- [Disco generator](/blog/disco-generator) — flashes new random colors on every beat.

## References

- https://react.dev/reference/react/useEffect
- https://developer.mozilla.org/en-US/docs/Web/API/setInterval
- https://overreacted.io/making-setinterval-declarative-with-react-hooks/
